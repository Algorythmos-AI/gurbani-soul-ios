## What & why

## Scripture impact
- [ ] No change to how Gurmukhi is stored, searched or rendered
- [ ] OR the change is display-only and the verbatim text still reaches copy/share/search unchanged

## Verification
- [ ] `make gates` passes (vendored inputs, versions, iOS gates)
- [ ] `make parity` passes (Swift kit == golden contract)
- [ ] UI change: screenshots from `testCaptureScreens` attached
